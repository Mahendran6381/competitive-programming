#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int> arr;
    int n, c = 0;
    while (cin >> n)
    {
        if (n == 4 || n == 7)
        {
            c = n;
        }
        if(c = 4 || c==7 && n==4 || o)
    }
    if (c >= 2)
    {
        std::cout << "YES" << std::endl;
    }
    else
    {
        std::cout << "NO" << std::endl;
    }
}